"use client";

import { Match } from "@/types/types";

function BreakButton({ match }: { match: Match }) {
	void match;
	// Legacy component retained for potential future time-out features.
	return null;
}

export default BreakButton;
